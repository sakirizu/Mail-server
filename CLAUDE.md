# SMAIL 技術仕様書（AI向け）

> **最終更新**: 2025-12-18 12:40
> **バージョン**: 1.1.0

---

## 📌 概要

- **プロジェクト名**: SMAIL (SSM Mail System)
- **説明**: セキュアなメールアプリケーション（2FA対応、AIフィッシング検知機能付き）
- **技術スタック**:
  - **フロントエンド**: React Native + React Navigation 7.x
  - **バックエンド**: Node.js + Express (port 3002)
  - **データベース**: MySQL (ユーザー/認証) + MongoDB (メールデータ)
  - **認証**: JWT + 2FA (TOTP, WebAuthn, バックアップコード)
  - **AI機能**: Python フィッシング検知サービス (port 5000, JPCERT/CC連携)

---

## 🏗️ アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                    クライアント (React Native)               │
│  ├── screens/        (画面コンポーネント)                    │
│  ├── components/     (UI コンポーネント)                     │
│  ├── context/        (状態管理: AuthContext)                │
│  └── services/       (API通信)                              │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP
┌─────────────────────────────────────────────────────────────┐
│                 バックエンド (Express.js: port 3002)         │
│  ├── server.js               (メインサーバー)                │
│  ├── services/
│  │   ├── mongoMailService.js (MongoDB メール操作)           │
│  │   ├── hybridMailService.js(ハイブリッドストレージ)        │
│  │   └── twoFactorAuth.js    (2FA認証ロジック)              │
│  └── checkPhishing()         (Python API呼び出し)           │
└─────────────────────────────────────────────────────────────┘
         ↓                              ↓
┌─────────────────────┐      ┌─────────────────────┐
│   MySQL (port 3306)  │      │  MongoDB (port 27017)│
│  - users             │      │  - mails             │
│  - user_2fa_settings │      │  - drafts            │
│  - totp_secrets      │      │  - attachments       │
│  - backup_codes      │      │  - statistics        │
│  - webauthn_creds    │      │                      │
└─────────────────────┘      └─────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│         フィッシング検知サービス (Python Flask: port 5000)    │
│  ├── JPCERT/CC 連携 (9,000+ URLs)                           │
│  ├── 類似URL検出 (Levenshtein距離)                          │
│  ├── 危険キーワード分析 (日本語/英語)                        │
│  └── 危険度スコアリング (0-100%)                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 認証・認可

### JWT認証
- **シークレット**: `process.env.JWT_SECRET`
- **有効期限**: 7日間
- **ミドルウェア**: `auth()` 関数で Bearer Token を検証

### 2FA (二段階認証)
| 方式 | 実装状況 | 説明 |
|------|---------|------|
| TOTP | ✅ 完了 | Google Authenticator 等対応 |
| WebAuthn | ✅ 完了 | 生体認証/セキュリティキー |
| バックアップコード | ✅ 完了 | 10個の緊急用コード |

### 認証フロー
```
1. POST /api/auth/login (ユーザー名/パスワード)
   ↓ 成功時
2. tempToken 発行 (2FA必要な場合)
   ↓
3. POST /api/auth/verify-2fa (TOTP/WebAuthn/バックアップ)
   ↓ 成功時
4. 本認証 JWT token 発行
```

---

## 📧 メール機能

### フォルダ構成
| フォルダ | API | 説明 |
|---------|-----|------|
| 受信トレイ | `GET /api/mails/inbox` | 受信メール |
| 送信済み | `GET /api/mails/sent` | 送信メール |
| 下書き | `GET /api/mails/drafts` | 下書き保存 |
| 迷惑メール | `GET /api/mails/spam` | フィッシング判定されたメール |
| ゴミ箱 | `GET /api/mails/trash` | 削除メール |

### メール送信フロー（フィッシング検知統合）
```
1. POST /api/mail/send (メール送信リクエスト)
   ↓
2. checkPhishing() 呼び出し
   → Python API (port 5000) に送信
   ↓
3. 危険度スコア取得 (0-100%)
   ↓
4. フォルダ振り分け
   - スコア >= 50 → spam フォルダ
   - スコア < 50 → inbox フォルダ
   ↓
5. MongoDB に保存 (phishing情報付与)
```

### メールデータ構造
```javascript
{
  _id: ObjectId,
  from: "sender@ssm.com",
  to: ["recipient@ssm.com"],
  cc: [],
  bcc: [],
  subject: "件名",
  body: "本文",
  date: ISODate,
  isRead: false,
  isStarred: false,
  isSpam: false,       // フィッシング判定
  labels: ["inbox"],   // or ["spam"]
  
  // フィッシング検知結果 (新規追加)
  phishing: {
    checked: true,
    score: 72,         // 0-100
    level: "WARNING",  // SAFE|CAUTION|WARNING|DANGER
    levelDisplay: "🟠 Warning",
    reasons: [
      "Similar to \"amazon.co.jp\" (85.2% match)",
      "Dangerous keywords: urgent, verify"
    ],
    checkedAt: ISODate
  }
}
```

---

## 🛡️ フィッシング検知サービス

### アーキテクチャ
```
backend/phishing-detector/
├── phishing_detector.py    # Flask サービス
├── requirements.txt        # Python依存関係
├── start.bat               # Windows起動スクリプト
├── README.md               # ドキュメント
├── jpcert_cache.json       # JPCERT/CCキャッシュ（自動生成）
└── phishtank_cache.json    # PhishTankキャッシュ（自動生成）
```

### API エンドポイント

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/` | ダッシュボード（HTML） |
| GET | `/health` | ヘルスチェック |
| POST | `/check-email` | メールのフィッシングチェック |
| POST | `/check-url` | 単一URLのチェック |
| POST | `/refresh-data` | フィッシングデータ更新 |
| GET | `/stats` | 統計情報 |

### メールチェック リクエスト/レスポンス
```javascript
// リクエスト
POST /check-email
{
  "from": "support@amazon-co-jp.xyz",
  "subject": "urgent account locked",
  "body": "click here https://amazon-secure.xyz/login"
}

// レスポンス
{
  "success": true,
  "is_phishing": true,
  "risk_score": 72,
  "risk_level": "WARNING",
  "risk_level_display": "🟠 Warning",
  "reasons": [
    "Similar to \"amazon.co.jp\" (82.5% match)",
    "Suspicious TLD: .xyz",
    "Dangerous keywords: urgent, click here"
  ],
  "recommendation": "spam"
}
```

### 検出ロジック（スコア計算）

| 検出項目 | スコア |
|----------|--------|
| 既知フィッシングURL (JPCERT/CC) | +50 |
| 類似ドメイン検出 (80%以上) | +30 |
| 送信者ドメイン偽装 | +20 |
| IPアドレスのURL使用 | +15 |
| 怪しいTLD (.xyz, .tk等) | +10 |
| 危険キーワード | +1〜4 (最大30) |

### 危険度レベル
| スコア | レベル | 表示 | 対応 |
|--------|--------|------|------|
| 0-29 | SAFE | 🟢 Safe | inbox へ |
| 30-49 | CAUTION | 🟡 Caution | inbox へ（警告表示） |
| 50-69 | WARNING | 🟠 Warning | spam へ |
| 70-100 | DANGER | 🔴 Danger | spam へ |

---

## 📱 フロントエンド

### 画面構成
| 画面 | ファイル | 説明 |
|------|---------|------|
| ログイン | `LoginScreen.js` | 2FA対応ログイン |
| 受信トレイ | `InboxScreen.js` | メール一覧 |
| メール詳細 | `MailDetailScreen.js` | フィッシング警告表示 |
| メール作成 | `ComposeScreen.js` | 新規メール作成 |
| 設定 | `ProfileScreen.js` | プロフィール/2FA設定 |
| 統計 | `StatisticsScreen.js` | メール/検知統計 |

### フィッシング警告UI (MailDetailScreen.js)
```javascript
// メール詳細画面でのフィッシング警告表示
{mail.phishing && mail.phishing.score > 0 && (
  <View style={[
    styles.phishingBanner,
    mail.phishing.score >= 70 ? styles.phishingDanger :
    mail.phishing.score >= 50 ? styles.phishingWarning :
    mail.phishing.score >= 30 ? styles.phishingCaution :
    styles.phishingSafe
  ]}>
    <Text>{mail.phishing.levelDisplay}</Text>
    <Text>Risk Score: {mail.phishing.score}%</Text>
    <Text>{mail.phishing.reasons.join(' • ')}</Text>
  </View>
)}
```

---

## 🔧 API一覧

### 認証
| メソッド | パス | 認証 | 説明 |
|---------|------|------|------|
| POST | `/api/auth/signup` | - | 新規登録 |
| POST | `/api/auth/login` | - | ログイン |
| POST | `/api/auth/verify-2fa` | - | 2FA認証 |
| GET | `/api/auth/verify` | ✅ | トークン検証 |

### メール
| メソッド | パス | 認証 | 説明 |
|---------|------|------|------|
| GET | `/api/mails/:folder` | ✅ | フォルダ別メール取得 |
| GET | `/api/mail/:mailId` | ✅ | メール詳細取得 |
| POST | `/api/mail/send` | ✅ | メール送信（フィッシングチェック付き） |
| PUT | `/api/mails/:mailId/read` | ✅ | 既読にする |

### フィッシング検知
| メソッド | パス | 認証 | 説明 |
|---------|------|------|------|
| POST | `/api/phishing/check` | - | メールチェック |
| GET | `/api/phishing/status` | - | サービス状態確認 |

---

## 🚀 起動手順

### 1. フィッシング検知サービス
```bash
cd backend\phishing-detector
.\start.bat
# http://localhost:5000 で起動
```

### 2. メールサーバー
```bash
cd backend
npm start
# http://localhost:3002 で起動
```

### 3. フロントエンド
```bash
npm start
```

---

## 📝 更新履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2025-12-18 | 1.1.0 | フィッシング検知AI実装、JPCERT/CC連携、UI警告表示追加 |
| 2025-12-18 | 1.0.0 | 初版作成 |
